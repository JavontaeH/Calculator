const subtract = (number, number2) => {
  const difference = number - number2;
  return difference;
};

const divide = (number, number2) => {
  const quotient = number / number2;
  return quotient;
};

const multiply = (number, number2) => {
  const product = number * number2;
  return product;
};

const square = (number) => {
  const squared = number * number;
  return squared;
};

let answer = divide(60, 2.5);
console.log(answer);
